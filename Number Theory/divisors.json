"Divisors": {
    "prefix": "Divisors",
    "body": [
      "vector<ll> Divisors;",
      "",
      "void Divisor(ll n)",
      "{",
      "    Divisors.clear();",
      "",
      "    for (int i = 1; (i * i) <= n; i++)",
      "    {",
      "        if (n % i == 0)",
      "        {",
      "            Divisors.push_back(i);",
      "            if (n / i != i)",
      "            {",
      "                Divisors.push_back(n / i);",
      "            }",
      "        }",
      "    }",
      "",
      "    sort(Divisors.begin(), Divisors.end());",
      "}"
    ],
    "description": "Divisors"
  }